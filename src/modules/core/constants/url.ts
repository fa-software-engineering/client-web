export const BASE_HTTP_URL = `http://${window.location.host}`;
export const BASE_WS_URL = `ws://${window.location.host}`;

export const API_URL = `${BASE_HTTP_URL}/api`;
export const GQL_URL = `${BASE_HTTP_URL}/graphql`;
export const AUTH_URL = `${BASE_HTTP_URL}/auth`;

export const API_WS_URL = `${BASE_WS_URL}/api`;
export const GQL_WS_URL = `${BASE_WS_URL}/graphql`;
